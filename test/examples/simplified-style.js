define(function (require) {

    var b = require('b'),
        e = {
          // many a complex item ere so it be
          f: function() {
            
          }
        },
        c = require('c'),
        d = 3;

    require('d');

});
